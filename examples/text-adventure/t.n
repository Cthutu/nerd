--

use std.random
use std.collections
use std.io

main :: fn () {
    random_number := random_u64()
    prn($"Random number: {random_number}")

    stack : Stack[i32]

    stack.push(42)
    stack.push(13)

    prn($"Popped: {stack.pop()}")
    prn($"Popped: {stack.pop()}")
}
