-- Standard I/O module.
--
-- This module now loads through the normal module pipeline.

ffi "nrt" {
    nrt_read_line (^u8, usize) -> usize
}

STDOUT_FILENO :: 1
STDERR_FILENO :: 2

input_buf : [256]u8
input_len : usize

pub input :: fn (prompt: string = "> ") -> string {
    pr(prompt)
    input_len = nrt_read_line(input_buf[..].data, input_buf[..].count)
    return input_buf[0..input_len].as(string)
}
