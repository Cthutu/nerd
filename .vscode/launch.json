{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Executable (Linux)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/_bin/nerd-debug",
            "args": [
                "build",
                "-v",
                "nerd-src/main.n"
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Compile"
        },
    ]
}
