long_default::fn(value:usize)=>value
init::fn(arena:Self,reserved_size:usize=long_default(4),grow_rate:usize=16,alloc_granularity:usize=4096){
}
¬
long_default :: fn (value: usize) => value

init :: fn (arena: Self,
            reserved_size: usize = long_default(4),
            grow_rate: usize = 16,
            alloc_granularity: usize = 4096) {
}
