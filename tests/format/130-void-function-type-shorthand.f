Sink :: fn (value: i32)

App :: plex {
    init fn (app:^App)
    logic fn (app:^App, delta_time: f32)
}
¬
Sink :: fn (value: i32)

App :: plex {
    init fn (app:^App)
    logic fn (app:^App, delta_time: f32)
}
