Point::plex{x i32 y i32 name string}

main::fn(){
p:Point=Point{x:1,y:2,name:"p"}
prn($"{p.name} {p.x} {p.y}")
}
¬
Point :: plex { x i32 y i32 name string }

main :: fn () {
    p : Point = Point { x: 1, y: 2, name: "p" }
    prn($"{p.name} {p.x} {p.y}")
}
