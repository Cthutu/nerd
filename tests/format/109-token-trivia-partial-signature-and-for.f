main::fn(a:u64,
-- keep parameter comment
b:

loop::fn(){
for index,item in values{
-- keep loop comment
item.count+=1
¬
main :: fn (a: u64,
-- keep parameter comment
b:

loop :: fn () {
    for index, item in values {
        -- keep loop comment
        item.count += 1
