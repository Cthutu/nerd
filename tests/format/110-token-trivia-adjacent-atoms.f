winsize::plex#c{
ws_row i16
ws_col i16
}

ffi "c" ioctl :: fn (fd: i32, request: i32, ...) -> i32

main :: fn () {
    prn("a" + "b"
}
¬
winsize :: plex #c {
    ws_row i16
    ws_col i16
}

ffi "c" ioctl :: fn (fd: i32, request: i32, ...) -> i32

main :: fn () {
    prn("a" +"b"
}
