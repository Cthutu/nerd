Display :: trait{show::fn(Self)->string}
Hasher :: trait {
hash :: fn(Self)->u64
finish :: fn()->u64
}
¬
Display :: trait {
    show :: fn (Self) -> string
}

Hasher :: trait {
    hash   :: fn (Self) -> u64
    finish :: fn () -> u64
}
