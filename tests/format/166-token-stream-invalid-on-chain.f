main :: fn () {
vao = on VAO.new().expect("Failed to create VAO")
ebo = EBO.new(indicies).expect("Failed to create EBO")
}
¬
main :: fn () {
    vao = on VAO.new().expect("Failed to create VAO")
    ebo = EBO.new(indicies).expect("Failed to create EBO")
}
