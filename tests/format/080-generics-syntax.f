id::fn[T](value:T)=>value
Box::plex[T]{value T}
Either::union[T,E]{ok T err E}
Maybe::enum[T]{None Some(T)}
pair:Pair[i32,string]
¬
id :: fn [T] (value: T) => value

Box :: plex [T] {
    value T
}

Either :: union [T, E] {
    ok  T
    err E
}

Maybe :: enum [T] { None Some(T) }

pair: Pair[i32, string]
