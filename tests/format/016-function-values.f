adder:=fn(a:i32,b:i32)=>a+b
main::fn(){
add::fn(a:i32,b:i32)=>a+b
return add(20,22)
}
¬
adder := fn (a: i32, b: i32) => a + b

main :: fn () {
    add :: fn (a: i32, b: i32) => a + b

    return add(20, 22)
}
