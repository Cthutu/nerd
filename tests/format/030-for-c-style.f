use mod std.print

main :: fn () {
for i := 0, step := 1; i < 5; total += step, i += 1 { prn($"C {i}") }
}
¬
use mod std.print

main :: fn () {
    for i := 0, step := 1; i < 5; total += step, i += 1 {
        prn($"C {i}")
    }
}
