-- Empty line inserted between fn and non-fn binding.
message::$"Hello,{name}! {value.as(u8)}"
main::fn()=>message
¬
-- Empty line inserted between fn and non-fn binding.
message :: $"Hello,{name}! {value.as(u8)}"

main :: fn () => message
