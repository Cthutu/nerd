-- Empty line inserted between fn and non-fn binding.
message::$"Hello,{name}! {value.cast(u8)}"
main::fn()=>message
¬
-- Empty line inserted between fn and non-fn binding.
message :: $"Hello,{name}! {value.cast(u8)}"

main :: fn () => message
